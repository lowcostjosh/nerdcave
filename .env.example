# Optional. When set, the Socratic thinking partner and turn analysis run on
# the Claude API (claude-opus-4-8). When unset, the platform runs entirely on
# the built-in deterministic demo engine — no credentials needed.
ANTHROPIC_API_KEY=

# Optional. Override where the SQLite database file lives (default: ./data/cognitiveos.db).
# COGNITIVEOS_DB_PATH=
